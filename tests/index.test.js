// const main = require('../src').main;

// Tests below is dependent on libraries included in demo.html - commenting out
// until we can find out how to include those.

// Example
describe('main', () => {
	test('pass', () => {
		expect(true);
	});

	/*
	test('should throw error when called with wrong signature', () => {
		expect(() => {
			// testing with all falsy values
			main('', 0, null, undefined, []);
		}).toThrowError('Call main with correct signature');
	});
	*/
});
